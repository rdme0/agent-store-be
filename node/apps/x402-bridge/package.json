{
  "name": "@agent-store/x402-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --env-file-if-exists=../../.env --import tsx src/index.ts",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@agent-store/agent-protocol": "0.1.0",
    "@x402/core": "2.22.0",
    "@x402/evm": "2.22.0",
    "@x402/fetch": "2.22.0",
    "fastify": "^5.12.0",
    "viem": "2.55.17"
  }
}
