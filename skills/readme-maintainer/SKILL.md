---
name: readme-maintainer
description: Keep AgentStore Kotlin/Spring, Node x402 bridge, demo-agent, database, and verification instructions synchronized.
---

# README Maintainer

Document the three local processes (Spring `8080` after cutover, bridge `8091`, demo agents `8090`), profiles, `.env`,
Flyway baseline/migrate behavior, simulated versus x402 mode, secret handling, and exact Gradle/npm verification
commands. Do not claim Base Sepolia smoke success without funded configuration.
