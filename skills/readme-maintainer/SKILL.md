---
name: readme-maintainer
description: Keep AgentStore Kotlin/Spring native x402, demo-agent, database, and verification instructions synchronized.
---

# README Maintainer

Document the Spring process (`8080`) and the optional independent Go demo-agent (`8090`), profiles,
`.env`, Flyway baseline/migrate behavior, simulated versus native x402 mode, Spring-only hot-wallet
secret handling, and exact Gradle/Go verification commands. The demo-agent owns its own `.env.example`
and README; backend documentation only links to it. Do not claim Base Sepolia smoke success without
funded configuration.
